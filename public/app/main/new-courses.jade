.panel.panel-primary
    .panel-heading.text-center New Courses
    .panel-body
        .row(ng-repeat="course in courses | orderBy:published:reverse | limitTo:10")
            .col-md-3 {{course.published | date:'MMM d'}}
            .col-md-6 {{course.name}}
